export interface Person {
  PersonID?: number;
  FirstName: string;
  LastName: string;
  Gender: 'Male' | 'Female' | 'Other';
  DateOfBirth: Date;
  // Address?: string;
  // City?: string;
  // State?: string;
  // ZipCode?: string;
  // Country?: string;
  // ContactNumber?: string;
  // Email?: string;
  // RegistrationDate?: Date;
}
